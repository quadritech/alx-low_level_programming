#include <stdio.h>
/**
 *main - entry point
 *Description: print alphabets in lower case except q&e
 *Return: 0
 */
int main(void)
{

char lettiers = 'a';

while (letters <= 'z')
{
	if (letters == 'e' || letters == 'q')
	{
	}
	else
	{
	putchar(letters++);
}
putchar('\n');
return (0);
}
