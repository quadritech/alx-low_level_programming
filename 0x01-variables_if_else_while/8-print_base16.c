#include <stdio.h>
/**
 *main - entry point
 *Description: print alphabets in lower case and upper case
 *Return: 0
 */
int main(void)
{

int num = 0;

while ( <= 9)
{
	putchar(num);
	num++;
}
num = 'a';
while (small <= 'f')
{
	putchar(num);
	num++;
}
putchar('\n');
return (0);
}
