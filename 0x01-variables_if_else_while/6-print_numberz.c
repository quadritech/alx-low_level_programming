#include <stdio.h>
/**
 * main - entry point
 * to print all single digit of base 10
 * Return: 0
 */

int main(void)
{
int num = 48;

while (num <= 57)
{
	putchar(num);
	num++;
}
putchar('\n');
return (0);
}
